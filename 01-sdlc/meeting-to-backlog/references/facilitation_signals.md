# Facilitation Signals

- `decision` or `approved` usually means a decision-log entry.
- `risk`, `blocker`, or `dependency` usually becomes a risk item.
- `need`, `must`, `should`, or `action` usually becomes a backlog or follow-up item.
