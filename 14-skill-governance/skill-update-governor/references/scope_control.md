# Scope Control

- target skill files are allowed by default
- `_skill-governance/update_requests/` is allowed for request registration
- governance-wide files are read surfaces, not default write surfaces
- any proposed file outside the target skill directory must be justified explicitly
