# Scoped Change Rules

- default scope is the target skill directory only
- governance files may be referenced, but not changed automatically from a normal skill improvement request
- changes to shared update policy require explicit governance review
