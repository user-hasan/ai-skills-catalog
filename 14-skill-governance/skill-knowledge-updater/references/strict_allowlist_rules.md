# Strict Allowlist Rules

- use only domains declared in `_skill-governance/source_policies.json`
- reject unknown domains by default
- if the source type is not covered by policy, stop and escalate rather than guessing
