# Release Signal Rules

- `major`: breaking API, data migration, removed behavior, incompatible config.
- `minor`: new user-facing capability, opt-in feature, additive integration.
- `patch`: bug fix, docs update, operational refinement, non-breaking polish.
