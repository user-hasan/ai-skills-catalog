# Tracker Mapping

- `title` maps to the issue or page title everywhere.
- `status` should map to tracker states such as todo, in-progress, blocked, done.
- `owner` should be kept symbolic until a real tracker user is chosen.
- `priority` should remain normalized as low, medium, high unless the target system needs numbers.
