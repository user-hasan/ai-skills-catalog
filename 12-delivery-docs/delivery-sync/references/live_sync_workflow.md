# Live Sync Workflow

- Generate the sync payload first.
- Confirm the target workspace or repository.
- Apply updates in one system at a time.
- Record what was created or updated so the next run can deduplicate.
